server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/candenizgumus/BurgerStore_Config.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "candenizgumus"
          password: ${JAVA14SOCIALMEDIATOKEN}